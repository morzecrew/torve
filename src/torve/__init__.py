"""Torve — deterministic gates for agent and human pull requests.

This package is the RFC 0002 increment: gates as an installed library,
no runner, no store, no sandbox, no agents.
"""

__version__ = "0.1.0"
