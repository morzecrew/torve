{
  "$defs": {
    "Gate": {
      "additionalProperties": false,
      "description": "One entry in gates.yaml (S-0002/the-gate-contract, lifecycle \u00a77 per S-0002/A-2).\n\n`run` is a shell command, or an `@`-prefixed builtin reference. The RFC's\n`@task.acceptance` is the acceptance builtin; the other builtins follow the\nsame convention (`@scope`, `@secrets`, ...). `commands` is the acceptance\nfallback for runs with no task file.\n\n`state` and `origin` are required on every entry (S-0002/D-19): a boolean\ncannot express shadow or quarantine, and provenance is unrecoverable\nlater. `shadow` and `quarantined` gates run and report but never affect\nthe exit code (\u00a77.3).\n\n`axis` is the optional conviction label (S-0034/D-4): what a red result from\nthis gate means. An entry without it reads as `functional` once\n`resolved_gates()` fills the default; the declaration itself stays absent\nso the manifest diff shows only labels the operator chose.\n\n`paths` is what the gate judges (S-0071/D-6) \u2014 the globs whose files it\nreads. Measured: `coverage-delta` measures `--cov=src` and spent 27% of its\ntwelve hours on attempts whose governed paths were nowhere under `src/`,\nseventeen of which went red over drift the attempt had not caused. An\nentry declaring no paths runs always.\n\n`sabotage` names this gate's twin \u2014 a CASES family in the sabotage suite\nor a repository test path \u2014 the evidence that the gate can convict (S-0036/D-3).\nThe load refuses a twinless entry once the manifest names a twin for any\ngate; a manifest naming none at all predates the field and is only voiced\nby `twinless_gates()`. The value must be a non-blank reference; resolving\nit against the CASES families and the tree cannot happen here \u2014 the\nsabotage suite imports this model, so the cross-check lives in the\nrepository test that pins the shipped battery.",
      "properties": {
        "axis": {
          "anyOf": [
            {
              "enum": [
                "functional",
                "boundary",
                "compliance",
                "form"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The optional conviction label (S-0034/D-4): what a red result from this gate means.\nDerived for unlabeled entries, functional.",
          "title": "Axis"
        },
        "commands": {
          "description": "The acceptance fallback for runs with no task file; only applies to\n@acceptance.",
          "items": {
            "type": "string"
          },
          "title": "Commands",
          "type": "array"
        },
        "input": {
          "anyOf": [
            {
              "enum": [
                "worktree",
                "diff",
                "task",
                "log"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Derived for builtins; defaults to worktree for shell gates.",
          "title": "Input"
        },
        "name": {
          "description": "The entry's name, unique across the manifest \u2014 how a gate is reported and\nquarantined.",
          "title": "Name",
          "type": "string"
        },
        "origin": {
          "description": "structural | leak/<task> | a citation (S-0059/D-4) \u2014 why this gate exists. Required\non every entry (S-0002/D-19): provenance is unrecoverable later.",
          "title": "Origin",
          "type": "string"
        },
        "paths": {
          "description": "What this gate judges (S-0071/D-6). Globs; the gate runs only when the\nattempt's diff touches one, and is reported `skipped` when it does not. An\nentry declaring none runs on every attempt, which is the default a gate\nwithout a subject wants.",
          "items": {
            "type": "string"
          },
          "title": "Paths",
          "type": "array"
        },
        "run": {
          "description": "A shell command, or an `@`-prefixed builtin reference such as `@task.acceptance` or\n`@scope`.",
          "title": "Run",
          "type": "string"
        },
        "sabotage": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The twin's CASES family or test path (S-0036/D-3) \u2014 the evidence that this gate can\nconvict. Must be a non-blank reference.",
          "title": "Sabotage"
        },
        "state": {
          "description": "Required on every entry (S-0002/D-19): `shadow` and `quarantined` gates run and\nreport but never affect the exit code (\u00a77.3).",
          "enum": [
            "shadow",
            "blocking",
            "quarantined"
          ],
          "title": "State",
          "type": "string"
        },
        "timeout": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Seconds; derived for builtins, 600 for shell gates.",
          "title": "Timeout"
        }
      },
      "required": [
        "name",
        "run",
        "state",
        "origin"
      ],
      "title": "Gate",
      "type": "object"
    },
    "Scope": {
      "additionalProperties": false,
      "description": "allow/deny globs, gitwildmatch semantics. deny wins over allow; an empty\nallow means unconstrained (S-0002/scope-in-detail).",
      "properties": {
        "allow": {
          "description": "Globs of what the task may touch, gitwildmatch; empty means unconstrained.",
          "items": {
            "type": "string"
          },
          "title": "Allow",
          "type": "array"
        },
        "deny": {
          "description": "Globs the task may not touch; deny wins over allow.",
          "items": {
            "type": "string"
          },
          "title": "Deny",
          "type": "array"
        }
      },
      "title": "Scope",
      "type": "object"
    },
    "SecretsConfig": {
      "additionalProperties": false,
      "description": "`allow_patterns` are regexes that suppress a match on the line they\nmatch. Reviewed configuration, not a bypass: the manifest arrives in a\npull request, and S-0002/D-8 stays intact because no signature at run time can\nwiden it.",
      "properties": {
        "allow_patterns": {
          "description": "Regexes that suppress a secrets match on the line they match.",
          "items": {
            "type": "string"
          },
          "title": "Allow Patterns",
          "type": "array"
        }
      },
      "title": "SecretsConfig",
      "type": "object"
    },
    "TestsConfig": {
      "additionalProperties": false,
      "properties": {
        "patterns": {
          "description": "The globs that say which files are tests, for the no-test-tampering gate.",
          "items": {
            "type": "string"
          },
          "title": "Patterns",
          "type": "array"
        }
      },
      "title": "TestsConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "gates": {
      "description": "The battery itself: every gate entry, run in the order the resolver puts them\nin.",
      "items": {
        "$ref": "#/$defs/Gate"
      },
      "title": "Gates",
      "type": "array"
    },
    "quarantine": {
      "description": "Acceptance commands that flake; their failures are recorded but stop blocking until\nfixed (S-0002/three-outcomes-gates-need-beyond-pass-and-fail). Maintained by humans\nfrom the flake counters in telemetry until a store exists (S-0003).",
      "items": {
        "type": "string"
      },
      "title": "Quarantine",
      "type": "array"
    },
    "schema_version": {
      "default": 1,
      "description": "The engine's shape version this manifest is read under.",
      "title": "Schema Version",
      "type": "integer"
    },
    "scope": {
      "$ref": "#/$defs/Scope",
      "description": "The repository-wide scope the scope gate judges a diff against when the run carries\nno task file of its own."
    },
    "secrets": {
      "$ref": "#/$defs/SecretsConfig",
      "description": "The reviewed allowances the secrets gate honours."
    },
    "telemetry": {
      "default": ".torve/telemetry.jsonl",
      "description": "The path, relative to the repository root, the gate run appends its telemetry\nrecords to.",
      "title": "Telemetry",
      "type": "string"
    },
    "tests": {
      "$ref": "#/$defs/TestsConfig",
      "description": "Which files count as tests, for the no-test-tampering gate."
    }
  },
  "title": "Manifest",
  "type": "object"
}
