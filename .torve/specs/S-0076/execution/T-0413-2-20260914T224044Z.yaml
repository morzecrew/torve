# yaml-language-server: $schema=../../../schemas/landing.json
task: T-0413
phase: 1
attempt: 2
base: 37db953fd59ee3068aee1e36ad5bab7e8bb2f21d
at: '2026-09-14T22:40:44Z'
agent: subscription/claude-opus-5@claude-opus-5
decisions:
  - id: D-1
    grade: ASSUMED
  - id: D-2
    grade: ASSUMED
  - id: D-3
    grade: LOCKED
  - id: D-4
    grade: ASSUMED
  - id: D-5
    grade: ASSUMED
entries:
  - decision: D-3
    grade: LOCKED
    at: '2026-09-14T22:33:42Z'
    claim: >-
      the hand-over lands with both of D-3's numbers still measurable: the orientation
      class is read from the trace per call and the pack side of this change adds
      no branch that could hide a read, so the revert condition stays judgeable from
      the same telemetry
    evidence: >-
      src/torve/application/contextpack.py:469-497 — render_index stops naming the
      handed-over files; src/torve/adapters/agent/harness.py:62-85 names the set the
      prompt carries, so what moved out of reads is exactly enumerable
    action: decided
  - decision: D-3
    grade: LOCKED
    at: '2026-09-14T22:38:40Z'
    attempt: 2
    claim: >-
      the hand-over leaves both of D-3's numbers measurable: what moved out of the
      read path is the five files the adapter enumerates and nothing else, so the
      orientation class stays readable from the trace per call and the revert condition
      (orientation falls while attempts per landing rises) is judged from the same
      telemetry that motivated the change
    evidence: >-
      src/torve/application/contextpack.py:471-500 — render_index names only decisions.json
      and the schemas as still behind a read; src/torve/adapters/agent/harness.py:71-83
      enumerates the five handed-over files one by one, so the set that left the read
      path is exactly stateable and neither side adds a branch that could hide a read
      from the trace
    action: decided
