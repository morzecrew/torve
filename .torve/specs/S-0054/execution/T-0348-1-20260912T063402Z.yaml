# yaml-language-server: $schema=../../../schemas/landing.json
task: T-0348
base: 0a2bb078c3a557e5f217561bf8b31ec2f5032833
commit: 9512e3a
at: '2026-09-12T06:34:02Z'
agent: session/claude-opus-5
decisions:
  - id: D-6
    grade: LOCKED
  - id: D-10
    grade: LOCKED
  - id: D-1
    grade: LOCKED
entries:
  - decision: D-6
    grade: LOCKED
    at: '2026-09-12T06:28:06Z'
    claim: >-
      The row says the section renders the standing rows whose paths reach a directory,
      and the implementation read  at document granularity only — a superseded document
      was skipped and a superseded row was not. Nothing is broken today because no
      live row carries the field, which is exactly why it was worth closing now: the
      first row to use it would have rendered beside its replacement, and a projected
      page has no column for 'this one is no longer the rule'. The reader would be
      left arbitrating two LOCKED rows over one directory.
    evidence: >-
      src/torve/application/colocation.py:122 — the row-level skip, beside the document-level
      one that was already there.
    action: decided
    proposal: >-
      None. D-6 already means the rows that stand; the code now agrees with it.
  - decision: D-10
    grade: LOCKED
    at: '2026-09-12T06:28:19Z'
    claim: >-
      The pack is deterministic, model-free and byte-identical for a replay, and all
      of that still holds — the change is to what one file *says*, not to how it is
      computed. A row's  carried every change entry the document recorded against
      it, and the amend verb writes a  stamp alongside every real change. So a third
      of what the pack offered as a rule's history was two hashes. Determinism was
      never the problem; signal-to-noise in a deterministic artefact was.
    evidence: >-
      src/torve/application/contextpack.py:97 — the field filter on a row's change
      entries; 17 of the 53 change entries this corpus holds are fingerprints.
    action: decided
    proposal: >-
      None for D-10. If anything, D-11's file list is the place to say what a pack
      file may carry, not only which files exist.
  - decision: D-1
    grade: LOCKED
    at: '2026-09-12T06:28:19Z'
    claim: >-
      The row ends 'the fingerprinted set stays text, grade and paths' — it settles
      what the fingerprint covers and is silent on whether the stamp itself is shown
      to anyone. The pack was showing it, as a change entry indistinguishable from
      a regrade. Reading D-1 as the fingerprint being bookkeeping for  rather than
      content for an executor is what this takes: the stamp proves a row was not hand-edited,
      and it proves it to the check, not to the agent.
    evidence: >-
      src/torve/application/contextpack.py:97 — amended_by keeps grade, paths and
      text changes and drops the stamp that accompanies each.
    action: decided
    proposal: >-
      None. D-1 stands; the fingerprint is the check's evidence and never the executor's.
