# yaml-language-server: $schema=../../schemas/execution.json
landings:
  - task: T-0296
    phase: 1
    commit: 32240b22338593a35cf196607b03d7427c5ff873
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-1
        grade: LOCKED
        at: '2026-09-09T13:50:42Z'
        claim: >-
          InheritedDecision gains consequence and check as the row says, and beside
          them check_state and check_twin, because the runner reads the contract alone
          and the promotion state and twin S-0054/D-4 puts on the row have to travel
          with it; all four default so every committed contract loads unchanged and
          no schema number moved; inherit_decisions copies them from the model through
          the same load_fences the loader uses, decision.recorded carries consequence
          and check, and the prompt renders the consequence after each row
        evidence: >-
          src/torve/domain/task.py:55 - the four fields with defaults; src/torve/adapters/agent/harness.py:131
          - the rendered lines; tests/test_intake.py adoption expectations carry the
          four
        action: decided
        notes: >-
          the record re-records every standing row once on the next import, because
          a version without a consequence differs from one with it — a one-time version
          2 across the corpus, not a regrade
      - decision: D-2
        grade: LOCKED
        at: '2026-09-09T13:50:43Z'
        claim: >-
          run_gates appends one decision:<id> shell gate per inherited row with a
          check, axis compliance, origin rfc/<doc>#<id>, input worktree, at the row's
          check_state; built from the contract in the runner and never written into
          the manifest
        evidence: >-
          src/torve/gates/runner.py:82 - decision_gates over ctx.task.decisions; tests/test_gates.py
          pins shadow-convicts-nothing and blocking-reds-the-pass
        action: decided
      - decision: D-3
        grade: LOCKED
        at: '2026-09-09T13:50:43Z'
        claim: >-
          owed() skips a row that carries a check and reports it as covered, whether
          the check will run green or red; a row with no check keeps the silence check;
          torve log owed prints the covered and pathless states beside what is owed
        evidence: >-
          src/torve/gates/decisions_reported.py:348 - the covered branch; tests/test_cli.py
          pins the three states
        action: decided
      - decision: D-4
        grade: LOCKED
        at: '2026-09-09T13:50:43Z'
        claim: >-
          a decision gate is shadow unless the row's check_state says blocking, and
          a blocking row without a check_twin is refused at mint by inherit_decisions;
          the twin rides the gate as its sabotage reference; torve gates check does
          not run decision twins in this phase — no row carries a check yet, and the
          twin's existence is the planner's refusal, not the battery's
        evidence: >-
          src/torve/application/planner.py:322 - the refusal; src/torve/gates/runner.py:82
          - state and sabotage from the row
        action: decided
        proposal: >-
          when the first row is promoted, extend torve gates check to run the promoted
          rows' twins over the contracts in .torve/tasks; until then the rule is enforced
          where the row is minted
  - task: T-0297
    phase: 2
    commit: e1d883abf273b6d9c48588d678e34c0b4685b80f
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-6
        grade: LOCKED
        at: '2026-09-09T14:08:34Z'
        claim: >-
          torve spec project renders one managed section per governed directory —
          rows with grade, text, paths, consequence, check and state, the invariants
          over it, the contended paths from the telemetry window — and a root index;
          the corpus path, the archive and every hidden directory are skipped because
          S-0016/D-25 admits nothing else in the corpus directory and the engine's
          own state is not where an agent works; twenty-one directories of this repository
          carry a section now
        evidence: >-
          src/torve/application/colocation.py:186 - render_section; src/torve/application/colocation.py:81
          - the projectable rule; src/torve/cli/AGENTS.md:1 - a rendered section
        action: decided
      - decision: D-7
        grade: LOCKED
        at: '2026-09-09T14:08:34Z'
        claim: >-
          the sections are committed with the code and the spec-projection shell gate
          runs torve spec project --check at shadow (S-0002/D-18) with tests/test_colocation.py
          as its twin; a hand edit inside the markers is drift naming the file, exit
          3
        evidence: >-
          .torve/gates.yaml:135 - the gate entry; src/torve/application/colocation.py:388
          - project with check; tests/test_colocation.py pins the drift case
        action: decided
      - decision: D-9
        grade: LOCKED
        at: '2026-09-09T14:08:34Z'
        claim: >-
          torve spec show, paths, tests and why-not read the worktree's corpus and
          archive through the loader and nothing else; there is no form that opens
          the record, and a corpus that does not load is a configuration error
        evidence: >-
          src/torve/cli/spec.py:167 - show over the loaded corpus; tests/test_cli_spec.py
          pins each verb over a corpus with an archive
        action: decided
      - decision: D-8
        grade: ASSUMED
        at: '2026-09-09T14:08:35Z'
        claim: >-
          a section carries rows, invariants, checks, the owed-entry reminder on a
          LOCKED row without a check, and contention; no overview and no prose beyond
          the row's own text and consequence
        evidence: >-
          src/torve/application/colocation.py:186 - the section's lines
        action: decided
  - task: T-0298
    phase: 3
    commit: c9502453902acc5d6afd9c263b791ddf504f83dd
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-10
        grade: LOCKED
        at: '2026-09-09T14:18:44Z'
        claim: >-
          the pack is built host-side from the record and the tree by pure builders
          and materialised into the worktree's .torve/context before the prompt is
          written, beside the skills; it ignores itself in git so it reaches no diff,
          scope verdict or landing in any repository; a shadow run receives the time-invariant
          files only — attempts and contention read the stream as it stands now —
          so a replay reads what the live attempt could have read
        evidence: >-
          src/torve/application/session.py:344 - materialised beside the skills; src/torve/application/contextpack.py:422
          - the self-ignoring write; src/torve/application/contextpack.py:350 - the
          replay rule
        action: decided
        notes: >-
          byte-identical for a shadow run is delivered by omission of the two time-varying
          files rather than by freezing them per attempt; freezing would need the
          pack stored with the trace, which S-0039's retention would then govern
      - decision: D-12
        grade: ASSUMED
        kind: departed
        at: '2026-09-09T14:18:45Z'
        claim: >-
          the red reaches the retry without a runner change: the runner already writes
          every gate result with its output into the telemetry row, and the pack's
          attempts.json reads the last red row for this task back — output tail, the
          decision ids a decisions-reported or scope output names, the failed test
          ids a pytest tail lists; JUnit is not wired, since the acceptance commands
          live in the manifest this phase does not touch, and the byte-loss marker
          is a heuristic over the shell layer's own '… truncated …' text rather than
          a count the row carries
        evidence: >-
          src/torve/application/contextpack.py:262 - _red_gates over the telemetry
          rows; tests/test_contextpack.py pins governing ids and failed tests from
          a red row
        action: departed
        proposal: >-
          the evidence document types the gate result with the bytes dropped and the
          JUnit ids; until then the pack reads the row the runner writes and runner.py
          stays untouched
      - decision: D-11
        grade: ASSUMED
        at: '2026-09-09T14:18:45Z'
        claim: >-
          the files are index.md, decisions.json, gates.json, tests.json, attempts.json,
          contended.json and schema/; tests.json carries per-file coverage under the
          scope from coverage.xml and the test modules that name each module, because
          the battery records no per-test contexts yet; every file ships in this phase
          and the per-file config-eval that keeps or removes each is the operator's
          run after it, as the phase intent says
        evidence: >-
          src/torve/application/contextpack.py:190 - tests_file over coverage.xml
          and the T-0113 rule; tests/test_contextpack.py pins the coverage read
        action: decided
  - task: T-0299
    phase: 4
    commit: a152e60dcc00cd06fd4e31b4bdeeb5b4d24e2cfb
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-13
        grade: LOCKED
        at: '2026-09-09T14:33:47Z'
        claim: >-
          the reviewer's copy receives the target's pack built as a replay — decisions,
          gates, tests and the schemas, no attempts and no contention, so no model-authored
          entry enters — plus touched.json: the contract rows whose paths the diff
          intersects, the last battery's coverage of the changed lines, and the findings
          prior reviews recorded on this target; the review role's own skill set replaces
          the executor's in the copy; the prompt names the pack and the schema file
          instead of restating the shape
        evidence: >-
          src/torve/application/review.py:656 - touched.json joins the replay pack
          before the copy is sealed
        action: decided
        notes: >-
          prior findings are listed as recorded; whether each was addressed is not
          computed — the engine has no cheap ground truth for it, and a heuristic
          over the diff would be a claim the reviewer should make, not read
      - decision: D-14
        grade: ASSUMED
        kind: departed
        at: '2026-09-09T14:33:47Z'
        claim: >-
          the default review skill set is [ratchet-what-you-build] alone: reading-isnt-proof
          is vendored in this repository's .torve/skills-vendor, not shipped as package
          data, and a package default naming it would refuse every adopter's review
          at materialisation
        evidence: >-
          src/torve/config/runconfig.py:888 - the shipped skill declaring the role
          is the default; the vendored one is added per repository through skills.sets
          or tier.skills
        action: departed
        proposal: >-
          S-0054/D-14 should read: the default review skill set names the shipped
          skills declaring the role; a repository adds its vendored ones
      - decision: D-15
        grade: ASSUMED
        at: '2026-09-09T14:33:48Z'
        claim: >-
          a findings or drafts document the engine finds but the model refuses raises
          SchemaRefusal naming the first failing field and the count of others; the
          drafter sees it on the next attempt as a lint refusal, the reviewer's single
          attempt escalates naming it; neither is recorded as unparseable, which stays
          the word for no document at all; the schemas ride the pack under schema/
        evidence: >-
          src/torve/application/review.py:358 - the refusal names the field; src/torve/application/intake.py:1538
          - the drafter's retry carries it
        action: decided
      - decision: D-16
        grade: ASSUMED
        at: '2026-09-09T14:33:48Z'
        claim: >-
          the drafter's prompt carries the pack's index.md and tells the drafter to
          run torve spec paths over each directory a draft would touch; the 400-path
          listing is gone; the pack is materialised into the drafter's read-only tree
          before the sandbox is created; composed without a pack the prompt names
          only the top-level entries
        evidence: >-
          src/torve/application/intake.py:1062 - the index stands where the listing
          stood
        action: decided
