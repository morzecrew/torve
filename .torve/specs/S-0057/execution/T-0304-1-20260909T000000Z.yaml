# yaml-language-server: $schema=../../../schemas/landing.json
task: T-0304
phase: 1
commit: 16655e5fbeb10a7434a117d9fb3c3b67f9c33244
at: '2026-09-09T00:00:00Z'
agent: session/claude-fable-5.1
entries:
- decision: D-2
  grade: LOCKED
  at: '2026-09-09T17:06:28Z'
  claim: A section is key and md; heading_of renders the heading from the key and render numbers
    by position; check_sections refuses an empty body, a typed-kind fence, the decisions table
    header and an amendment identifier as a key, naming the section; a plain yaml fence that
    illustrates a shape stays prose
  evidence: src/torve/config/spec.py:520 - check_sections; src/torve/domain/spec.py:182 -
    heading_of
  action: decided
- decision: D-3
  grade: LOCKED
  at: '2026-09-09T17:06:28Z'
  claim: The corpus is .torve/specs and the archive and schemas are its siblings, derived
    from the corpus path so a relocated corpus carries them; the configuration key is specs.path
    with layout.SPECS_DIR as its default and a before-validator refuses the rfcs key naming
    specs; every reader that opened config.rfcs.path opens config.specs.path
  evidence: src/torve/config/runconfig.py:908 - SpecsConfig and the refusal of rfcs; src/torve/config/spec.py:99
    - archive_dir and schemas_dir as siblings
  action: decided
- decision: D-9
  grade: LOCKED
  at: '2026-09-09T17:06:30Z'
  claim: config/spec.py changed for the directory shape only; the tree scan of citations is
    phase 4's and nothing of it was written here
  evidence: src/torve/config/spec.py:1 - no scan of tracked files
  action: decided
- decision: D-5
  grade: LOCKED
  at: '2026-09-09T17:07:00Z'
  claim: torve init writes the four document schemas into .torve/schemas from the model, each
    cut to the fields its file carries with the shared definitions kept whole; every file's
    first line names its own schema two levels up, the same from the corpus and the archive;
    spec check warns when a schema is not written yet and reddens when one lags the model
  evidence: src/torve/cli/init.py:20 - init_cmd; src/torve/config/spec.py:153 - schema_text
    per file
  action: decided
  notes: phase 2 grows init to the contract, the log, the configuration, the manifest and
    the ignore file; phase 1 writes only what the document needs
- decision: D-1
  grade: LOCKED
  at: '2026-09-09T17:07:20Z'
  claim: A specification is the directory S-NNNN of document.yaml, decisions.yaml, amendments.yaml
    and execution.yaml; FILE_FIELDS in the model says which key each file carries and the
    loader refuses a key in the wrong file by name; an absent file is an empty list; the loader
    joins the four into the one Document and every reader — planner, colocation, pack, importer,
    show — reads what it read; schema_version 3, and 2 is refused naming the phase-1 conversion
  evidence: src/torve/config/spec.py:240 - load_document joins the files a directory holds;
    src/torve/domain/spec.py:270 - FILE_FIELDS
  action: decided
  notes: 'the directory carries the identifier and nothing else (S-0057/A-2): the slug said
    what document.yaml and spec list already say; document.yaml and decisions.yaml are always
    written so a new document shows where the rows go, the tool''s and the landing''s files
    appear with their first entry and never shrink to nothing'
- decision: D-4
  grade: LOCKED
  at: '2026-09-09T17:07:20Z'
  claim: torve spec absorbs every torve rfc verb in one module and cli/rfc.py is deleted;
    the two show verbs merged into the lookup one, whose payload is a superset; rfc schema
    became torve init; the gate is spec-valid in the manifest and the pack; rfc_emit.py is
    spec_emit.py; the skill is spec-writer with the symlink following; new prose says specification
    or document and old prose keeps its word
  evidence: src/torve/cli/spec.py:1 - the merged module; .torve/gates.yaml:68 - spec-valid
  action: decided
  notes: 'domain/rfc.py keeps its module name: it owns the vocabularies, is imported from
    eleven modules, and S-0057/D-4 names the emitter and the CLI only — renaming it is a follow-up
    if anyone wants it; test file names keep rfc per §4'
- decision: D-6
  grade: LOCKED
  at: '2026-09-09T17:07:21Z'
  claim: 'Every live and archived document converted once from a schema-2 dump taken at the
    branch point: 312 refused or empty sections dropped, 56 typed fences and 56 decisions
    tables removed, headings and amended_by dropped, the model written as four files into
    .torve/specs and .torve/archive as S-NNNN directories; rfcs/, archive/rfcs/ and rfcs/schema/
    deleted in the same commit; a parity script compared 2,649 surviving items with 0 mismatches
    and is not committed, as 0056''s was not; rotted globs in the live rows (rfcs/**, archive/**,
    rfcs/schema/**, rfc_emit.py, cli/rfc.py) relocated to their new homes through relocate_paths'
  evidence: '`uv run python scratchpad/parity57.py` — documents 57 items 2649 mismatches 0'
  action: decided
  notes: the sixteen task logs fold in phase 3 when the Landing model exists; the corpus-bootstrap
    fixture converted with the corpus into fixtures/S-0001
