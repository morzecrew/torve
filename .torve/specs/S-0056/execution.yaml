# yaml-language-server: $schema=../../schemas/execution.json
landings:
  - task: T-0300
    phase: 1
    commit: 08c5d79126e8707416c1b3957a15c46a400c4daa
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-1
        grade: LOCKED
        at: '2026-09-09T15:32:01Z'
        claim: >-
          a document is one YAML file NNNN-slug.yaml in the Document model's own field
          order, loaded by Document.model_validate over yaml.safe_load plus the corpus
          checks the model cannot express; schema_version 2, and 1 is refused naming
          the conversion; the markdown parser, load_fences, the heading and table
          regexes and the fenced kinds are deleted and every reader of the corpus
          reaches the model through the same load_corpus
        evidence: >-
          src/torve/config/spec.py:121 - the loader is the validator; a version other
          than 2 is refused before validation
        action: decided
      - decision: D-2
        grade: LOCKED
        at: '2026-09-09T15:32:01Z'
        claim: >-
          DecisionDetail is gone; a row carries rationale, cites, check, check_state,
          check_twin, superseded_by and a stored fingerprint stamp; inherit_decisions
          takes the Document and reads the row; the frontmatter fingerprint map is
          gone and fingerprint_drift compares the row's stamp to the row
        evidence: >-
          src/torve/application/planner.py:283 - inherit_decisions over the model's
          rows
        action: decided
      - decision: D-3
        grade: LOCKED
        at: '2026-09-09T15:32:02Z'
        claim: >-
          DesignSection is key, heading and md; level and order are dropped and the
          list's order is the order; markdown inside md is never parsed, only searched
          for citations, links and line numbers with fenced blocks stripped
        evidence: src/torve/domain/spec.py:171 - the section model
        action: decided
      - decision: D-4
        grade: LOCKED
        at: '2026-09-09T15:32:02Z'
        claim: >-
          every writer mutates the loaded model and writes it through dump_document,
          one SafeDumper with the model's key order, literal blocks for strings holding
          a newline, folded blocks for strings over 48 characters so no plain scalar
          wraps, flow lists for short identifier lists; comments are not preserved
          and check refuses one outside the header line, found by probing each candidate
          line — dropping it leaves the loaded document unchanged, which a hash line
          inside a block scalar never does; fmt is --check only and reports drift
          without failing on it
        evidence: >-
          src/torve/config/spec.py:424 - the comment probe; src/torve/config/rfc_emit.py:128
          - the one serializer
        action: decided
        notes: >-
          fmt --check exits 0 on drift and 3 only on a document the loader refuses:
          a hand-authored document is legal as it stands, so drift is information,
          not a failure; fix_row_text keeps the markdown-era guard and accepts the
          hand-edited text as it now reads when the row is unstamped, refusing only
          a row already stamped as it is
      - decision: D-5
        grade: LOCKED
        at: '2026-09-09T15:32:02Z'
        claim: >-
          every document converted once: the old loader dumped all 56 documents as
          JSON, the new model dumped them as YAML, the markdown files, the parser,
          the table emitter and INDEX.md were deleted in the same change, and a parity
          script compared every item of every document — 2,786 items, 0 mismatches
          — before anything was committed; the corpus-bootstrap fixture converted
          the same way
        evidence: >-
          src/torve/config/spec.py:468 - the directory check names a leftover markdown
          file and INDEX.md as problems
        action: decided
        notes: >-
          the parity test ran as a pre-commit script from a scratch directory rather
          than as a committed pytest, because the loader it needs is deleted by the
          same commit it gates; its result is recorded here and the live-corpus canonical-form
          test in tests/test_rfc_emit.py is what stays. One defect: the JSON dump
          was taken before S-0056/A-2 was written, so the converted 0056 lacked it
          and the first YAML amend appended a duplicate S-0056/A-2 — restored by hand
          in S-0056/A-3
      - decision: D-7
        grade: ASSUMED
        at: '2026-09-09T15:32:03Z'
        claim: >-
          INDEX.md is deleted and torve rfc list answers in phase 1, since a corpus
          without the index has no other listing; rfc render waits for phase 2 as
          the row's phasing places it
        evidence: src/torve/cli/rfc.py:818 - the list verb
        action: decided
      - decision: D-8
        grade: ASSUMED
        at: '2026-09-09T15:32:03Z'
        claim: >-
          SKILL.md, AGENTS.md, the colocated sections and the pack are untouched;
          the projection renders from the same model and its tests pass unchanged
          except for the document filename suffix in one expectation
        evidence: >-
          src/torve/application/colocation.py:202 - the section renders from the model's
          row and document
        action: decided
      - decision: D-6
        grade: LOCKED
        at: '2026-09-09T15:38:37Z'
        claim: >-
          the schema header line is already written on every document by the serializer
          and refused nowhere; rfcs/schema/document.json, the rfc schema verb, its
          drift check and rfc new's 0055 shape are phase 2's unit as the phasing places
          them; cli/rfc.py changed here only for the verbs over the model
        evidence: >-
          src/torve/config/spec.py:106 - schema_header names the file phase 2 writes
        action: decided
      - decision: D-9
        grade: LOCKED
        at: '2026-09-09T15:38:38Z'
        claim: >-
          the planner still writes contracts to files: it changed here only to admit,
          plan and reconcile over the loaded model instead of the frontmatter; minting
          into the record and projecting at dispatch is phase 3's unit
        evidence: >-
          src/torve/application/planner.py:283 - inherit_decisions over the model;
          write_contracts unchanged
        action: decided
      - decision: D-10
        grade: LOCKED
        at: '2026-09-09T15:38:38Z'
        claim: >-
          no task directory is deleted here; the only task files this change touches
          are T-0300's own contract, re-minted over the scope S-0056/A-2 and S-0056/A-3
          widened, and its log; the deletion of every archived document's task directory
          is phase 3's unit
        evidence: >-
          .torve/tasks/T-0300/contract.yaml:1 - the re-minted contract, minted by
          the phase-1 header
        action: decided
  - task: T-0301
    phase: 2
    commit: c9e65b8466661ebfd7dcb2404fc372a4c3a20e73
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-6
        grade: LOCKED
        at: '2026-09-09T15:48:44Z'
        claim: >-
          torve rfc schema writes rfcs/schema/document.json as the model's JSON Schema;
          check reports a missing file as a warning naming the verb and a file that
          lags the model as a problem; every document's first line already names it;
          rfc new writes 0055's shape — header keys, one summary section, empty lists
          — with the header line
        evidence: >-
          src/torve/config/spec.py:122 - check_schema; src/torve/cli/rfc.py:819 -
          the schema verb
        action: decided
        notes: >-
          a missing schema is a warning, not a problem: a corpus in a fixture or a
          fresh repository has not written one yet and the header line does not need
          the file to exist for the loader
      - decision: D-7
        grade: ASSUMED
        at: '2026-09-09T15:48:45Z'
        claim: >-
          rfc render NNNN writes the document as a markdown page — header facts, the
          prose in order, the rows as a table with their details, invariants, alternatives,
          questions, phasing, amendments — to stdout or --out; it reads the corpus
          and the archive and writes nothing into either; rfc list landed in phase
          1
        evidence: >-
          src/torve/config/rfc_emit.py:447 - render_markdown; src/torve/cli/rfc.py:860
          - the render verb
        action: decided
        notes: >-
          S-0056/Q-1 is settled by shipping render in phase 2: it cost one function,
          and the docs pages can be generated from it when wanted
      - decision: D-4
        grade: LOCKED
        at: '2026-09-09T15:48:45Z'
        claim: >-
          render_markdown is the second writer this row permits — the only markdown
          one — and it writes a page, never a document; every document writer is still
          dump_document
        evidence: >-
          src/torve/config/rfc_emit.py:447 - the markdown projection beside the one
          serializer
        action: decided
      - decision: D-1
        grade: LOCKED
        at: '2026-09-09T15:48:46Z'
        claim: >-
          the loader gains the schema file's location and text beside it; loading
          is unchanged
        evidence: >-
          src/torve/config/spec.py:114 - schema_text from the model
        action: decided
      - decision: D-5
        grade: LOCKED
        at: '2026-09-09T15:53:19Z'
        claim: >-
          rfcs/schema/document.json is generated output written beside the converted
          corpus, not a document; every document stays as phase 1 converted it
        evidence: >-
          rfcs/schema/document.json:1 - the model's JSON Schema, drift-checked
        action: decided
      - decision: D-10
        grade: LOCKED
        at: '2026-09-09T15:53:20Z'
        claim: >-
          only this task's own log and contract are touched; the task directory's
          fate is phase 3's
        evidence: >-
          .torve/tasks/T-0301/log.yaml:1 - the task's own log
        action: decided
  - task: T-0302
    phase: 3
    commit: 978ebb535bc7ad1f45e10dba1a21e35002794849
    at: '2026-09-09'
    agent: session/claude-fable-5.1
    entries:
      - decision: D-9
        grade: LOCKED
        at: '2026-09-09T16:08:46Z'
        claim: >-
          torve plan --partition P mints the planned contracts into P's record through
          the same mint the manager's importer records and writes no file; task numbers
          and prior mints are read from the board as well as the task directories;
          dispatch projects the board's contract into the worktree beside the skills
          and the pack, leaving a tracked contract alone, and the log written there
          is imported after the attempt as before; without a partition every path
          is unchanged
        evidence: >-
          src/torve/application/planner.py:560 - mint_contracts over residency.mint;
          src/torve/application/session.py:334 - the projection before the skills
        action: decided
        notes: >-
          the single-task fence in runner.py reads other active tasks' contracts from
          the root's task directory and skips a task with no file; under a store that
          fence is the board's overlaps check at dispatch, and torve run of one task
          loses the file fence for record-only siblings — runner.py is outside this
          phase's scope, noted for the operator
      - decision: D-10
        grade: LOCKED
        at: '2026-09-09T16:08:46Z'
        claim: >-
          the gitignore ignores .torve/tasks/ whole and the three exception rules
          go with it; every task directory whose contract names an archived document
          — 280 of 296 — is deleted and the sixteen under standing documents (T-0288
          to T-0303, with the three document-less fixes of 0053's own machinery) leave
          tracking; there is no retention leg over task directories in the reaper
          to delete — its sweeps are run state, worktrees and traces
        evidence: >-
          .gitignore:225 - the task directory ignored whole
        action: decided
        notes: >-
          the deletion itself and the untracking are the one destructive step of this
          phase and are done as a separate commit by hand
      - decision: D-2
        grade: LOCKED
        at: '2026-09-09T16:12:27Z'
        claim: >-
          inherit_decisions is untouched; the planner changed for the board-aware
          numbering and the contract writer only
        evidence: >-
          src/torve/application/planner.py:283 - inherit_decisions as phase 1 left
          it
        action: decided
