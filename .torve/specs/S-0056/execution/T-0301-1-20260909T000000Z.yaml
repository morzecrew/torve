# yaml-language-server: $schema=../../../schemas/landing.json
task: T-0301
phase: 2
base: 466179526c1b47ab59cc996e8c647918cc1adb91
commit: c9e65b8466661ebfd7dcb2404fc372a4c3a20e73
at: '2026-09-09T00:00:00Z'
agent: session/claude-fable-5.1
entries:
- decision: D-6
  grade: LOCKED
  at: '2026-09-09T15:48:44Z'
  claim: torve rfc schema writes rfcs/schema/document.json as the model's JSON Schema; check
    reports a missing file as a warning naming the verb and a file that lags the model as
    a problem; every document's first line already names it; rfc new writes 0055's shape —
    header keys, one summary section, empty lists — with the header line
  evidence: src/torve/config/spec.py:122 - check_schema; src/torve/cli/rfc.py:819 - the schema
    verb
  action: decided
  notes: 'a missing schema is a warning, not a problem: a corpus in a fixture or a fresh repository
    has not written one yet and the header line does not need the file to exist for the loader'
- decision: D-7
  grade: ASSUMED
  at: '2026-09-09T15:48:45Z'
  claim: rfc render NNNN writes the document as a markdown page — header facts, the prose
    in order, the rows as a table with their details, invariants, alternatives, questions,
    phasing, amendments — to stdout or --out; it reads the corpus and the archive and writes
    nothing into either; rfc list landed in phase 1
  evidence: src/torve/config/rfc_emit.py:447 - render_markdown; src/torve/cli/rfc.py:860 -
    the render verb
  action: decided
  notes: 'S-0056/Q-1 is settled by shipping render in phase 2: it cost one function, and the
    docs pages can be generated from it when wanted'
- decision: D-4
  grade: LOCKED
  at: '2026-09-09T15:48:45Z'
  claim: render_markdown is the second writer this row permits — the only markdown one — and
    it writes a page, never a document; every document writer is still dump_document
  evidence: src/torve/config/rfc_emit.py:447 - the markdown projection beside the one serializer
  action: decided
- decision: D-1
  grade: LOCKED
  at: '2026-09-09T15:48:46Z'
  claim: the loader gains the schema file's location and text beside it; loading is unchanged
  evidence: src/torve/config/spec.py:114 - schema_text from the model
  action: decided
- decision: D-5
  grade: LOCKED
  at: '2026-09-09T15:53:19Z'
  claim: rfcs/schema/document.json is generated output written beside the converted corpus,
    not a document; every document stays as phase 1 converted it
  evidence: rfcs/schema/document.json:1 - the model's JSON Schema, drift-checked
  action: decided
- decision: D-10
  grade: LOCKED
  at: '2026-09-09T15:53:20Z'
  claim: only this task's own log and contract are touched; the task directory's fate is phase
    3's
  evidence: .torve/tasks/T-0301/log.yaml:1 - the task's own log
  action: decided
