# yaml-language-server: $schema=../../schemas/amendments.json
amendments:
  - id: A-1
    at: '2026-09-20T16:28:21Z'
    md: |-
      Phase 1's acceptance runs `torve init`, and the verb rewrites every
      generated schema beside the corpus, not the one the phase adds a field
      to: T-0455's first attempt was convicted at the scope gate for the
      other five. The scope reaches `.torve/schemas/**`; nothing else in the
      phase changes.
      torve init rewrites every schema, so phase 1 reaches them all
  - id: A-2
    at: '2026-09-20T16:41:28Z'
    md: |-
      The warning D-1 adds is one every accepted design without `change`
      earns, and `tests/test_rfc_check.py` asserts the check's whole output
      for fixture documents that have none. T-0455's candidate passed its
      own acceptance and failed that file on the lane's full run. Phase 1
      reaches the file and runs it.
      Phase 1 reaches the check tests the new warning touches
