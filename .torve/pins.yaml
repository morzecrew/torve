# The ref this repository trusts for each fetched equipment source (S-0062/D-13).
# A profile names what it wants and omits the ref; this says which ref that source
# means, so a bump is one line rather than one line per profile that named it.
#
# Not an equipment layer: nothing here gives a seat anything it did not ask for
# (S-0062/A-6). A profile that writes its own `ref:` keeps it.
github:mattpocock/skills: 3cca18b368ae95cdbdebbff572ccafa662551015
