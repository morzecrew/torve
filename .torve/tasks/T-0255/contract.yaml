schema_version: 1
id: T-0255
rfc: rfcs/0036-test-evidence.md
phase: 2
role: implement
title: the sabotage pair lint
intent: 'Gate declarations carry `sabotage:` naming a CASES family or a test path; manifest load refuses
  a twinless gate (D-36.3); every existing entry in this repository''s gates.yaml is backfilled in
  the same landing so the lint ships with zero grandfather exceptions. A red case pins the refusal,
  a green one pins the shipped manifest. This repository''s own gates.yaml is NOT touched here — the
  gates run under the host engine, whose manifest model predates this task''s sabotage field (the
  self-hosting boundary, T-0215''s class; attempt 1 died proving it again): the schema and the refusal
  land with scratch-manifest tests, and the battery''s backfill follows as its own task once the schema-bearing
  engine is the one evaluating it.'
depends_on: []
parent: null
targets: []
scope:
  allow:
  - src/torve/config/manifest.py
  - tests/test_manifest.py
  deny: []
acceptance:
- uv run pytest tests/test_manifest.py
- uv run mypy src
- uv run basedpyright src
- uv run ruff check .
decisions:
- id: D-36.1
  grade: LOCKED
  text: The coverage-delta gate judges the attempt's changed lines only — never the inherited tree,
    never a whole-repo threshold — and enters shadow like every gate (D-2.18); promotion to blocking
    is a separate act on soak evidence
  paths:
  - .torve/gates.yaml
  - src/torve/gates/sabotage.py
- id: D-36.2
  grade: OPEN
  text: 'The changed-lines threshold and the exact pytest-cov/diff-cover invocation: the executor
    measures candidate values against recent landings and logs the settled command'
  paths:
  - .torve/gates.yaml
- id: D-36.3
  grade: ASSUMED
  text: A manifest gate entry names its sabotage twin (`sabotage:` — a CASES family or a test path);
    manifest load refuses a twinless gate, with existing entries backfilled in the landing so the
    lint ships with zero exceptions
  paths:
  - src/torve/config/manifest.py
  - .torve/gates.yaml
- id: D-36.4
  grade: LOCKED
  text: 'A fix for an escaped defect owes a review-corpus entry beside the fix: the defective diff
    from history, the finding a reviewer should have produced (written by a person, never generated),
    and the fixed tree — the corpus grows at the rate defects escape (0002''s accumulate-from-leaks
    doctrine applied to the reviewer)'
  paths:
  - .torve/review-corpus/**
  - src/torve/cli/review.py
- id: D-36.5
  grade: ASSUMED
  text: '`torve review corpus add <fixing-commit>` scaffolds the entry from the commit pair (defective
    landing by trailer, fix parent as tree); a commit with no shipped defective ancestor is refused
    with an instructive message'
  paths:
  - src/torve/cli/review.py
budget:
  iterations: null
  wallclock_minutes: null
  tokens: null
tier: executor
tier_variant: null
character: null
