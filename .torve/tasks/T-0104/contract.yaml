# Minted by `torve plan 0024` — phase 2: trust-classes-bind-capability
schema_version: 1
id: T-0104
rfc: rfcs/0024-fleet.md
phase: 2
role: implement
intent: 'Trust classes enforced against each root''s own configuration before the

  root is ticked, so that the capabilities the corpus already grants

  unevenly — socket mode, host networking, provider routing breadth, and

  RFC 0021''s broker mode where it exists — are refused when a repository

  asks for more than its class allows, naming the class and the setting.

  This is what turns D-17.10 from a sentence an operator remembers into a

  refusal an operator reads.'
depends_on:
- T-0103
targets: []
scope:
  allow:
  - src/torve/config/fleet.py
  - src/torve/application/fleet.py
  - tests/**
  deny: []
acceptance:
- uv run ruff check src tests
- uv run mypy src
- uv run basedpyright src
- uv run pytest
- uv run lint-imports
decisions:
- id: D-24.1
  grade: LOCKED
  text: The fleet manifest is operator-side and never lives in a repository under work
  paths:
  - src/torve/config/fleet.py
- id: D-24.2
  grade: LOCKED
  text: The escalation pause is decided once for the fleet and passed down; every other leg
    runs in every root
  paths:
  - src/torve/application/fleet.py
  - src/torve/application/loop.py
- id: D-24.3
  grade: LOCKED
  text: 'No fleet store: aggregation is read-only over roots, and no root''s truth ever lives
    outside it'
  paths:
  - src/torve/application/fleet.py
- id: D-24.4
  grade: ASSUMED
  text: Roots tick serially in a deterministic manifest order; `order` is never a priority
    field
  paths:
  - src/torve/application/fleet.py
  - src/torve/config/fleet.py
- id: D-24.5
  grade: ASSUMED
  text: A root that fails or is locked out is recorded and the pass continues
  paths:
  - src/torve/application/fleet.py
- id: D-24.6
  grade: ASSUMED
  text: Trust classes (`own`, `reviewed`, `untrusted`) bind per-repository capability, checked
    before a root is ticked; a root asking for more than its class allows is refused with
    the class and the setting named
  paths:
  - src/torve/config/fleet.py
  - src/torve/application/fleet.py
- id: D-24.7
  grade: ASSUMED
  text: Per-root locks stay; there is no fleet lock
  paths:
  - src/torve/application/fleet.py
- id: D-24.8
  grade: ASSUMED
  text: '`torve fleet status` orders by escalation age across roots — RFC 0006''s primary
    alert given its fleet form'
  paths:
  - src/torve/cli/fleet.py
- id: D-24.9
  grade: LOCKED
  text: A task belongs to exactly one root; no cross-repository task and no cross-root dependency
    exists
  paths:
  - src/torve/application/fleet.py
- id: D-24.10
  grade: OPEN
  text: Whether a root's own `loop.pause_escalations` still applies beneath the fleet threshold;
    execution decides and logs it
  paths:
  - src/torve/application/loop.py
- id: D-24.11
  grade: OPEN
  text: Where the fleet event is recorded — every ticked root's telemetry, or an operator-side
    stream beside the manifest
  paths:
  - src/torve/application/fleet.py
budget:
  iterations: null
  wallclock_minutes: null
  tokens: null
tier: executor
