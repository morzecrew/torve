# Adopted from T-0126's drafts (RFC 0020) — ids minted at adoption, D-20.4.
schema_version: 1
id: T-0128
role: implement
intent: Give the rfc-writer skill's template a concrete, valid 'Contract example'
  fenced YAML block (skills/rfc-writer/references/rfc-template.md, alongside the existing
  Phasing fence at lines 119-145) demonstrating the pattern authors copy into new
  RFCs, with no other new example tags added to the template; extend tests/test_skills.py
  with a case that reads the shipped template and runs it through the contract-example
  parser/checker so a schema change that breaks the template's own example is caught
  in CI, not just in a real RFC.
depends_on:
- T-0127
scope:
  allow:
  - skills/rfc-writer/references/rfc-template.md
  - tests/test_skills.py
  deny: []
acceptance:
- uv run ruff check src tests
- uv run pytest
decisions:
- id: D-25.1
  grade: LOCKED
  text: "The emitter and `fmt` normalise only the structures the parser models \u2014\
    \ frontmatter, decision tables, phasing fences, amendment headings; body prose\
    \ passes through byte-for-byte"
  paths:
  - src/torve/config/rfc_emit.py
  - src/torve/cli/rfc.py
- id: D-25.2
  grade: LOCKED
  text: "Every authoring verb is one transaction \u2014 parse, mutate, emit, regenerate\
    \ the index, check \u2014 and a red check aborts the whole write, leaving the\
    \ tree untouched"
  paths:
  - src/torve/cli/rfc.py
  - src/torve/config/rfc_emit.py
- id: D-25.3
  grade: LOCKED
  text: 'Verbs write structure and identifiers, never judgement: no verb chooses a
    grade, drafts decision or amendment prose, or flips `status`/`implementation`'
  paths:
  - src/torve/cli/rfc.py
- id: D-25.4
  grade: LOCKED
  text: Amendment numbers are derived at write time as maximum plus one corpus-wide,
    through the parser's `next_amendment`; the verb never accepts a chosen number
  paths:
  - src/torve/config/rfc_parse.py
  - src/torve/cli/rfc.py
- id: D-25.5
  grade: ASSUMED
  text: Storage stays one `NNNN-slug.md` per document; the authoring surface is the
    answer to structural fragility, and D-A.18 stands unamended
  paths:
  - rfcs/**
- id: D-25.6
  grade: ASSUMED
  text: "`rfc retire` executes D-16.1 whole \u2014 row removal, `retired:` frontmatter,\
    \ tombstone stub \u2014 and refuses while any citation would stop resolving"
  paths:
  - src/torve/cli/rfc.py
  - src/torve/config/rfc_emit.py
- id: D-25.7
  grade: ASSUMED
  text: '`rfc relocate-paths` sweeps Paths cells mechanically and never touches decision
    text; in-row markers stay hand-written per D-16.4'
  paths:
  - src/torve/cli/rfc.py
- id: D-25.8
  grade: OPEN
  text: Whether `fmt` renumbers sections or only validates numbering; execution proposes
    with evidence from the first documents touched
  paths:
  - src/torve/config/rfc_emit.py
- id: D-25.9
  grade: OPEN
  text: 'Whether a `schema_version: 2` section manifest per kind becomes checkable
    once the emitter exists; deferred until `fmt` has run over the corpus'
  paths:
  - src/torve/config/rfc_parse.py
- id: D-25.10
  grade: ASSUMED
  text: Typed example fences extend the phasing pattern one validator at a time, starting
    with `contract-example` against the task schema; no speculative tags
  paths:
  - src/torve/config/rfc_parse.py
tier: executor
rfc: rfcs/0025-corpus-authoring-surface.md
parent: T-0125
