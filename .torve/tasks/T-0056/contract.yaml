# Minted by hand — A-27's execution: the second live tick showed the
# reaper sweeping the lane's READY input; the order changes, nothing else.
schema_version: 1
id: T-0056
rfc: rfcs/0019-standing-loop.md
phase: 1
role: implement
intent: |
  The tick's fixed order becomes poll, lane, reap, dispatch, sync
  (amendment A-27): the lane runs before the reaper so ready candidates
  land before they are swept — A-26's merge-before-reap ordering applied
  inside the tick — while reap keeps its place ahead of dispatch so the
  scope-overlap check still sees a corpse-free picture. The order test
  pins lane-before-reap; no other tick semantics change.
depends_on: [T-0055]
targets: []
scope:
  allow:
    - "src/torve/application/**"
    - "tests/**"
  deny: []
acceptance:
  - "uv run ruff check src tests"
  - "uv run mypy src"
  - "uv run basedpyright src"
  - "uv run pytest"
  - "uv run lint-imports"
  - "uv run torve rfc check"
decisions:
  - id: D-19.3
    grade: ASSUMED
    text: "Tick order is fixed: poll, lane, reap, dispatch, sync. Amended by A-27 2026-08-24 — as accepted the order was reap-first, and the second live tick showed the reaper sweeping the lane's READY input; A-26's merge-before-reap applies inside the tick"
    paths:
      - src/torve/application/loop.py
budget: {}
tier: executor
