# Minted by the runner at gated — review follows execution.
id: T-0280
rfc: rfcs/0043-blocker-revision.md
role: review
intent: Review T-0279's diff against its contract and inherited decisions.
targets:
- T-0279
decisions:
- id: D-43.1
  grade: LOCKED
  text: A surviving review blocker spends the run's `blocker_revisions` budget (default
    1) as in-run attempts in the same worktree before escalating `blocker_finding`;
    the poison ceiling and task budgets count every revision, unchanged
  paths:
  - src/torve/application/runner.py
- id: D-43.2
  grade: LOCKED
  text: "The revision's critique travels only in the RFC 0005 \xA74a feedback record\
    \ \u2014 blockers rendered as threads (review id as author, claim + evidence as\
    \ body), the convicted diff as the superseded diff, cap and truncation honesty\
    \ unchanged"
  paths:
  - src/torve/application/feedback.py
  - src/torve/application/review.py
- id: D-43.3
  grade: ASSUMED
  text: "`blocker_revisions: 0` restores the pre-0043 transition sequence exactly\
    \ \u2014 configuration decides the consequence (D-2), and the conservative setting\
    \ is always reachable"
  paths:
  - src/torve/config/runconfig.py
- id: D-43.4
  grade: ASSUMED
  text: "Unparseable verdicts and broker budget refusals keep immediate escalation\
    \ \u2014 infrastructure and cost are not revisable defects"
  paths:
  - src/torve/application/runner.py
- id: D-43.5
  grade: ASSUMED
  text: The record is written root-side (`.torve/tasks/<id>/feedback.md`) so a post-escalation
    re-dispatch inherits the accumulated critique with zero new mechanism
  paths:
  - src/torve/application/feedback.py
- id: D-43.6
  grade: OPEN
  text: "Whether the revision attempt's history fact should carry a structured marker\
    \ (for the calibration projection to count revisions vs first attempts) or the\
    \ prose fact suffices \u2014 settled by whoever builds the first consumer"
  paths:
  - src/torve/application/runner.py
budget:
  iterations: 1
tier: reviewer
schema_version: 1
