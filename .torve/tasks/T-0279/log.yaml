schema_version: 1
task: T-0279
repo: morzecrew/torve
base_sha: ba6b0f08464029267208c4eed3c89e1d873a56aa
drift_count: 0
entries:
- decision: D-43.6
  grade: OPEN
  kind: resolved
  class: discovery
  at: '2026-09-04T17:42:49Z'
  attempt: 1
  claim: D-43.6 leaves open whether the revision history fact needs a structured marker or
    plain prose suffices; no consumer of task history parses this field today, and RFC 0043
    §5.1's own example fact carries no marker — so prose is decided as sufficient, matching
    the existing gate-red retry history entries' shape
  evidence: src/torve/application/runner.py:1728 — the revision history fact is a plain f-string,
    same shape as the pre-existing gate-red retry entry
  action: decided
- decision: D-43.1
  grade: LOCKED
  kind: resolved
  class: discovery
  at: '2026-09-04T18:38:32Z'
  attempt: 1
  claim: A surviving review blocker spends the run's blocker_revisions budget as in-run attempts
    in the same worktree before escalating blocker_finding; the poison ceiling and iteration
    budget are checked at the top of _attempt_loop before every dispatch, so a revision is
    counted as an attempt like any other and blocker_finding still fires once the budget is
    spent, unparseable, or a broker refusal occurs
  evidence: src/torve/application/runner.py:1746 — review_hook_fn spends _blocker_revisions_spent(state)
    against config.review.blocker_revisions and returns None to continue the loop in place;
    escalates blocker_finding at line 1774 once the budget is spent
  action: decided
- decision: D-43.2
  grade: LOCKED
  kind: resolved
  class: discovery
  at: '2026-09-04T18:38:38Z'
  attempt: 1
  claim: 'The revision''s critique travels only in the RFC 0005 §4a feedback record: blocker
    findings are rendered as threads with the review id as the sole comment''s author and
    the claim plus evidence as its body, the convicted candidate''s diff rides along as the
    superseded diff, and the existing cap and truncation honesty in render_feedback are unchanged
    and reused as-is'
  evidence: src/torve/application/review.py:389 — blocker_threads(blockers, review_id) builds
    one thread per blocker with review_id as the comment author and claim+evidence as the
    body, fed straight into feedback.capture_feedback with no new record shape
  action: decided
- decision: D-43.6
  grade: OPEN
  kind: resolved
  class: discovery
  at: '2026-09-04T18:38:51Z'
  attempt: 1
  claim: The revision attempt's history fact carries no structured marker — a prose fact with
    a fixed, greppable prefix suffices, since no consumer counting revisions vs first attempts
    exists yet; a structured field is left for whoever builds that projection, per the RFC's
    own deferral
  evidence: 'src/torve/application/runner.py:445 — _BLOCKER_REVISION_MARKER is a plain string
    prefix (''review blocker (revision ''), and the fact appended at line 1766 is prose (''review
    blocker (revision N of M): review_id: claims''), parsed only by the prefix match in _blocker_revisions_spent'
  action: decided
  proposal: keep the prose marker; add a structured field (e.g. a history entry key) only
    when the calibration projection needs to distinguish revisions from first attempts
