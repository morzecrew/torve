schema_version: 1
task: T-0027
repo: morzecrew/torve
base_sha: 531eb458bd899bddb555ef549d7eed2339417c94
drift_count: 0
entries:
  - decision: D-18.2
    grade: LOCKED
    kind: resolved
    at: 2026-08-22T21:20:00Z
    attempt: 1
    claim: every component lives in torve.cli.console and every touched file
      is under cli/ — application/projections.py returns the same dict it
      returned before this task, and no Rich renderable crosses the boundary
    evidence: src/torve/cli/console.py:103-158 — the component block; the
      diff touches only src/torve/cli/** and the two named test files
    action: decided
  - decision: D-18.4
    grade: LOCKED
    kind: resolved
    at: 2026-08-22T21:21:00Z
    attempt: 1
    claim: styling is applied via Text(style=...) and Table style parameters
      only; markup stays False on both consoles; every coloured distinction
      also carries a mark, a word or a column position
    evidence: src/torve/cli/console.py:38-62 — the style vocabulary and
      OUTCOME_STYLES beside OUTCOME_MARKS; grep for "[red]" style markup in
      src/torve/cli finds nothing
    action: decided
  - decision: D-18.1
    grade: LOCKED
    kind: resolved
    at: 2026-08-22T21:22:00Z
    attempt: 1
    claim: every JSON emission is byte-identical — no emit_json call site
      changed — and the updated tests assert human output by content only
      (section names, task ids, verdict words), never layout
    evidence: tests/test_context.py:114-138 — the three-format test; the
      existing JSON round-trip tests in tests/test_cli.py pass unchanged
    action: decided
  - decision: unlisted
    grade: UNLISTED
    kind: resolved
    class: discovery
    at: 2026-08-22T21:23:00Z
    attempt: 1
    claim: >-
      first execution of a planner-minted contract, and the minted scope
      taught its first lesson — it covers exactly what the Phasing entry
      declared (src/torve/cli/**, two test files), which excludes the
      repository's bookkeeping surfaces (CHANGELOG.md, the RFC's
      implementation flip): those ride a separate docs commit outside the
      task diff, and future Phasing entries should either include the
      bookkeeping globs or the convention should say bookkeeping is never
      part of a task's diff
    evidence: .torve/tasks/T-0027/contract.yaml:14-18 — the minted scope
    action: decided
    proposal: 0018 or 0016 row — minted scopes and repository bookkeeping;
      pick one convention (include the globs at phasing time, or keep
      bookkeeping in docs commits) and write it down
  - decision: unlisted
    grade: UNLISTED
    kind: resolved
    class: discovery
    at: 2026-08-22T22:10:00Z
    attempt: 2
    claim: >-
      two rendering defects the owner's first real-terminal run surfaced —
      the truncation and likely-landed notes were table rows in the first
      column, sizing the column to the note (the proposals table grew a
      gulf between "decision" and "from"); and 27 task ids in one cell is
      noise wearing data. add_rows_truncated now returns the withheld count
      and callers print a footer component after the table; id_list bounds
      comma lists to the first eight plus "(+N more)"
    evidence: src/torve/cli/console.py:129-150 — add_rows_truncated, footer,
      id_list; call sites in src/torve/cli/context.py
    action: decided
    proposal: 0018 row — notes about a table print after it via the footer
      component, never as rows; unbounded id lists render through id_list
  - decision: D-18.7
    grade: ASSUMED
    kind: resolved
    at: 2026-08-22T22:11:00Z
    attempt: 2
    claim: the live_status component lands — one transient spinner line with
      elapsed time on stderr (results stay on stdout, D-11.6), a no-op under
      --plain, CI and --format json by rule — and gates run wraps its gate
      pass in it, which is the 28-second wait the owner reported as reading
      like a hang
    evidence: src/torve/cli/console.py:178-196 — live_status;
      src/torve/cli/gates.py:78-79 — the wrap
    action: decided
    notes: |
      Coarse-grained on purpose: one line for the whole pass. Naming the
      currently-running gate needs a progress hook on gates/runner.py,
      which is outside this task's scope — proposed below.
    proposal: 0018 or 0002 row — run_gates grows an optional per-gate
      progress callback so the live status can name the gate it is inside;
      until then the status is one line for the whole pass
