# Minted by `torve plan 0036` — phase 2: escapes mint corpus entries
schema_version: 1
id: T-0256
rfc: rfcs/0036-test-evidence.md
phase: 2
role: implement
title: escapes mint corpus entries
intent: 'The corpus duty (D-36.4) lands as mechanism: torve review corpus add <fixing-commit>
  scaffolds an entry from the commit pair (D-36.5) — defective landing located by its task
  trailer, fix parent as the tree, the finding paragraph left explicitly for the operator
  with a refusing placeholder the loader rejects until written. A commit with no shipped defective
  ancestor is refused with an instructive message. The review page documents the duty.'
depends_on: []
parent: null
targets: []
scope:
  allow:
  - src/torve/cli/review.py
  - tests/test_review_corpus.py
  deny: []
acceptance:
- uv run pytest tests/test_review_corpus.py
- uv run mypy src
- uv run basedpyright src
- uv run ruff check .
decisions:
- id: D-36.1
  grade: LOCKED
  text: The coverage-delta gate judges the attempt's changed lines only — never the inherited
    tree, never a whole-repo threshold — and enters shadow like every gate (D-2.18); promotion
    to blocking is a separate act on soak evidence
  paths:
  - .torve/gates.yaml
  - src/torve/gates/sabotage.py
- id: D-36.2
  grade: OPEN
  text: 'The changed-lines threshold and the exact pytest-cov/diff-cover invocation: the executor
    measures candidate values against recent landings and logs the settled command'
  paths:
  - .torve/gates.yaml
- id: D-36.3
  grade: ASSUMED
  text: A manifest gate entry names its sabotage twin (`sabotage:` — a CASES family or a test
    path); manifest load refuses a twinless gate, with existing entries backfilled in the
    landing so the lint ships with zero exceptions
  paths:
  - src/torve/config/manifest.py
  - .torve/gates.yaml
- id: D-36.4
  grade: LOCKED
  text: 'A fix for an escaped defect owes a review-corpus entry beside the fix: the defective
    diff from history, the finding a reviewer should have produced (written by a person, never
    generated), and the fixed tree — the corpus grows at the rate defects escape (0002''s
    accumulate-from-leaks doctrine applied to the reviewer)'
  paths:
  - .torve/review-corpus/**
  - src/torve/cli/review.py
- id: D-36.5
  grade: ASSUMED
  text: '`torve review corpus add <fixing-commit>` scaffolds the entry from the commit pair
    (defective landing by trailer, fix parent as tree); a commit with no shipped defective
    ancestor is refused with an instructive message'
  paths:
  - src/torve/cli/review.py
budget:
  iterations: null
  wallclock_minutes: null
  tokens: null
tier: executor
tier_variant: null
character: null
