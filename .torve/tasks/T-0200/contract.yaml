# Minted by `torve plan 0032` — phase 1: serve-backend
schema_version: 1
id: T-0200
rfc: rfcs/0032-the-serve-surface.md
phase: 1
role: implement
title: serve backend
intent: 'The torve serve verb behind the torve[serve] extra: starlette app with /api/context
  and /api/status re-exposing the existing projections verbatim (D-32.1), loopback-only with
  no host flag (D-32.2), lazy-imported with an instructive refusal (D-32.3), instructive 404
  when no bundle is shipped. Endpoint tests via TestClient, import-guard test mirrored from
  the opensandbox pattern.'
depends_on: []
parent: null
targets: []
scope:
  allow:
  - src/torve/cli/serve.py
  - src/torve/cli/main.py
  - pyproject.toml
  - tests/test_serve.py
  deny: []
acceptance:
- uv run pytest tests/test_serve.py
- uv run mypy src
- uv run basedpyright src
- uv run ruff check .
decisions:
- id: D-32.1
  grade: LOCKED
  text: The server re-exposes existing projection functions verbatim and computes nothing
    of its own; a shape the browser needs is added to the projection, for every surface at
    once
  paths:
  - src/torve/cli/serve.py
- id: D-32.2
  grade: LOCKED
  text: 'v1 is read-only and loopback-only: no mutating endpoint, no host flag, no auth because
    no reachability; the write path arrives only with its own RFC'
  paths:
  - src/torve/cli/serve.py
- id: D-32.3
  grade: ASSUMED
  text: '`torve serve` lives behind the `torve[serve]` extra with starlette and uvicorn, lazy-imported
    with an instructive refusal — the opensandbox/migrate pattern'
  paths:
  - pyproject.toml
  - src/torve/cli/serve.py
- id: D-32.4
  grade: ASSUMED
  text: The frontend ships as built assets in wheel package data (`torve/_web`), built in
    CI by the organization's npm-builder image; runtime and install never touch node
  paths:
  - pyproject.toml
  - src/torve/cli/serve.py
- id: D-32.5
  grade: OPEN
  text: Whether the built bundle is vendored in-repo (hash-checked against source like INDEX.md)
    or CI-attached at wheel build; the first release build settles it
  paths:
  - pyproject.toml
- id: D-32.6
  grade: ASSUMED
  text: The page polls with a visible projected-at stamp; no push channel
  paths:
  - src/torve/cli/serve.py
budget:
  iterations: null
  wallclock_minutes: null
  tokens: null
tier: executor
tier_variant: null
