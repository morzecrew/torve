schema_version: 1
task: T-0292
repo: morzecrew/torve
base_sha: 60f665f0465511dbe7ce4da767c952a0be6e3f98
drift_count: 0
entries:
- decision: D-53.10
  grade: LOCKED
  kind: resolved
  at: '2026-09-09T09:58:58Z'
  attempt: 1
  claim: the row is satisfied by the loader's derivation, which torve rfc new already calls;
    the parser's own next_number stays corpus-only because phase 1's parity test pins that
    difference and phase 5 deletes the parser — A-140's wording named the parser's derivation,
    and this entry records that the loader's is the one in force
  evidence: src/torve/config/rfc_parse.py:168 - the docstring names the loader's derivation
    as the verb's; tests/test_rfc_archive.py pins 8 from the loader and 3 from the parser
    over the same tree
  action: decided
- decision: unlisted
  grade: UNLISTED
  kind: resolved
  at: '2026-09-09T09:58:58Z'
  attempt: 1
  claim: check_corpus resolves citations against what the archive defines and its retired
    lists, and a depends_on, informed_by or supersedes reference to an archived number is
    a warning naming the archive rather than a refusal; lookup falls back to the archive and
    marks the answer archived, so the MCP show face resolves what the CLI does
  evidence: src/torve/config/rfc_parse.py:1322 - the resolvable set gains the archive; src/torve/config/rfc_parse.py:1018
    - _lookup_in over corpus then archive
  action: decided
  proposal: 'no row needed: this is A-140''s unit executed as written; phase 5 carries the
    archive-aware resolution into the loader''s check when the parser is deleted'
