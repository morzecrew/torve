schema_version: 1
task: T-0244
repo: morzecrew/torve
base_sha: 4478245560583d25cf04092c1fff15500c41fe29
drift_count: 0
entries:
  - decision: D-42.1
    grade: LOCKED
    kind: resolved
    action: decided
    class: discovery
    at: 2026-09-04T06:52:41Z
    attempt: 1
    claim: assembly is the single composition root and every verb consumes it —
      RunDeps (build_run_deps), TickDeps (build_tick_deps /
      build_fleet_tick_deps), the intake deps (build_intake_deps) and the
      review leg (review_agent_for); run_cmd, tick_cmd and the fleet loop
      build no adapter inline and no fourth copy of the wiring exists
    evidence: src/torve/cli/assembly.py:124-212 — the bundle builders;
      src/torve/cli/fleet.py:102-107 is the fleet loop's single consuming
      call (the old `_build_deps`, src/torve/cli/fleet.py:90 at base_sha,
      is deleted); src/torve/cli/run.py:148-181 and src/torve/cli/tick.py:43-55
      are the other consumers
    notes: |
      build_tier_agent / build_reviewer_agent moved from cli/run.py to the
      composition root; cli/run.py re-exports them so the drafting, corpus
      and eval verbs (outside this task's paths) keep their imports, and
      the monkeypatch in test_review_corpus.py still lands on the name
      they import through.

  - decision: D-42.2
    grade: LOCKED
    kind: resolved
    action: decided
    class: discovery
    at: 2026-09-04T06:52:41Z
    attempt: 1
    claim: the extraction is behaviour-preserving and layering-preserving —
      same bundles, same ports, front-door checks (unknown agent, role
      dispatchability, sizing refusal, oversize override, require_ci
      without scm.repo) stay in the verbs, and the import-linter contract
      set is untouched (no file under imports checks was edited)
    evidence: >-
      `uv run lint-imports` — "Contracts: 5 kept, 0 broken";
      `uv run pytest tests/test_cli.py tests/test_fleet_cli.py
      tests/test_run_loop.py tests/test_tick.py tests/test_fleet.py` plus
      the full suite all exit 0; src/torve/cli/run.py:99-146 is the run
      verb's front door, src/torve/cli/tick.py:46-53 the tick's
    notes: |
      Assembly raises the plain ValueError for a require_ci gate with no
      remote (src/torve/cli/assembly.py:230-234); each consumer maps it as
      it always did — tick_cmd fails the door with exit 3, a fleet root
      records "error: …" and the pass continues.

  - decision: unlisted
    grade: UNLISTED
    kind: resolved
    action: decided
    at: 2026-09-04T06:52:41Z
    attempt: 1
    claim: RFC 0042 §10 left open whether the fleet's per-root loop consumes
      the assembly directly or through a thinner per-root handle; the
      actual shape of TickDeps reuse settled it — the fleet's tick_one is
      (root, config) → deps → run_tick with one extra kwarg, exactly the
      signature a direct build_fleet_tick_deps call fills, so no handle
      type would carry information a builder does not
    evidence: src/torve/cli/fleet.py:102-107 — tick_one, three statements
      against the base_sha copy at src/torve/cli/fleet.py:425-432 which
      differed only in calling the deleted _build_deps
    proposal: close RFC 0042 §10's first bullet as "the fleet consumes the
      composition root directly" — D-42.1's single-producer rule admits a
      thin handle only as a forwarding layer, and none was minted

  - decision: unlisted
    grade: UNLISTED
    kind: resolved
    action: decided
    at: 2026-09-04T06:52:41Z
    attempt: 1
    claim: the fleet loop's two divergences from the solo tick — no post-push
      forge bookkeeping (PR retirement, captured-thread replies) in its
      lane leg, and no dispatch envelope line — are kept as refused-by-name
      switches inside the one tick builder rather than unified; the
      deleted module's docstring sketched an upgrade path of "call the
      shared helper from both", but wiring them on changes what a fleet
      root sends to the forge and prints, which D-42.2 (LOCKED) forecloses
      for this extraction
    evidence: src/torve/cli/assembly.py:218-220 — forge_bookkeeping and
      dispatch_envelope on the shared _tick_deps, set by build_tick_deps
      (line 200) and build_fleet_tick_deps (line 212); the base_sha
      confession was src/torve/cli/fleet.py:6-16
    proposal: a decision row (amendment or new phase) on whether a fleet
      root's lane gains the solo tick's forge bookkeeping and the dispatch
      leg gains the envelope — behaviour changes, not refactors, each
      needing its own verdict; until then build_fleet_tick_deps is the
      single place that names the gap
  - decision: D-42.2
    grade: LOCKED
    kind: resolved
    action: decided
    class: discovery
    at: '2026-09-04T06:58:00Z'
    attempt: 2
    claim: this attempt repaired a rule violation the hoist carried with it — dispatch_agent_factory
      and route_dispatch_providers were nested in the base verb, where their docstrings' corpus
      coordinates were exempt; as public module-level functions in cli they enter the blocking
      user-facing-text gate's scope, so the two docstrings now say what the functions do and
      the decision identifiers moved to the comment above each def — no behaviour, wiring
      or bundle changed
    evidence: src/torve/cli/assembly.py:74-81 — the comment above the now-clean docstring
      of dispatch_agent_factory; `uv run python -c "from torve.gates.user_facing_text import
      _check_file; print(_check_file('src/torve/cli/assembly.py', open('src/torve/cli/assembly.py').read()))"`
      — prints an empty list
    notes: 'Discovery rather than drift: the working rule names the surfaces Typer renders,
      and no command renders these builders'' help — but the gate''s heuristic scans every
      public module-level function docstring under src/torve/cli/, so the hoist alone changed
      what is in scope. Attempt 1''s evidence cites assembly.py ranges this edit shifts by
      four lines: the bundle builders are 128-216 (cited 124-212), the named switches 222-224
      (cited 218-220), the require_ci ValueError 236-237 (noted 230-234). All six acceptance
      commands re-ran green after the edit.
  
      '
  - decision: D-42.1
    grade: LOCKED
    kind: resolved
    action: decided
    class: discovery
    at: '2026-09-04T06:58:00Z'
    attempt: 2
    claim: the single-producer fact still holds after this attempt's text edit — the composition
      root is the only builder of the dep bundles, run_cmd, tick_cmd and the fleet loop consume
      it, and no fourth copy of the wiring exists
    evidence: '`grep -rn "_build_deps" src/` — no matches; the bundle builders are src/torve/cli/assembly.py:128-216,
      consumed at src/torve/cli/run.py:148-181, src/torve/cli/tick.py:43-55 and src/torve/cli/fleet.py:102-107'
    notes: 'Boundary recorded, not crossed: run.py''s cancel still opens a store to deliver
      one durable cancel, and the intake and review verbs — outside this task''s declared
      paths — still build their one-shot adapters, with the agent builders they share now
      flowing from the composition root through cli/run''s re-exports. None of that is the
      (root, config) dep wiring this row names; it is per-call infrastructure the solo verbs
      have always held.
  
      '
