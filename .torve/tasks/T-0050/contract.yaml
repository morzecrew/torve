# Minted by `torve plan 0008` — phase 2: The GitHub Issues projection
schema_version: 1
id: T-0050
rfc: rfcs/0008-tracker-projection.md
phase: 2
role: implement
intent: 'The Tracker port and its first adapter: reflect maps engine states to

  issue state and labels and returns applied, refused or unsupported —

  a refusal is a logged divergence, never an exception; comments are

  one per attempt, keyed on task, state and attempt through the

  idempotency rule; findings annotate; escalations label from the

  enumerated vocabulary and assign. Inbound is the fixed command

  vocabulary — retry, abandon, unblock — parsed allow-listed from

  comments, validated against the real store, refusals posted back.

  Tracker text is untrusted input everywhere. Proven against the lab

  repository: all states projected, idempotency verified by

  deliberately replaying the relay, one refusal path exercised.'
depends_on:
- T-0049
targets: []
scope:
  allow:
  - src/torve/application/**
  - src/torve/adapters/**
  - src/torve/cli/**
  - src/torve/config/**
  - tests/**
  # Widened by hand (wiring seam, D-7.23 family): the new adapters.tracker
  # package joins the import-linter independence contract.
  - pyproject.toml
  deny: []
acceptance:
- uv run ruff check src tests
- uv run mypy src
- uv run basedpyright src
- uv run pytest
- uv run lint-imports
- uv run torve rfc check
decisions:
- id: D-8.1
  grade: LOCKED
  text: The tracker holds no authoritative state; leases, status and history live in the store
  paths:
  - src/torve/application/tracker.py
- id: D-8.2
  grade: LOCKED
  text: Projection rides the outbox, keyed on `(task_id, state, attempt)` for idempotency
  paths:
  - src/torve/application/outbox.py
  - src/torve/application/tracker.py
- id: D-8.3
  grade: LOCKED
  text: Inbound is a fixed command vocabulary validated against the store; a card move is
    an intent, not a state change
  paths:
  - src/torve/application/tracker.py
- id: D-8.4
  grade: LOCKED
  text: Task contracts are not editable from a tracker
  paths:
  - src/torve/application/tracker.py
- id: D-8.5
  grade: LOCKED
  text: Tracker text is untrusted input and is never treated as specification
  paths:
  - src/torve/application/tracker.py
  - src/torve/adapters/tracker/**
- id: D-8.6
  grade: ASSUMED
  text: '`reflect` returns applied/refused/unsupported; refusal is a logged divergence'
  paths:
  - src/torve/application/ports.py
  - src/torve/adapters/tracker/**
- id: D-8.7
  grade: ASSUMED
  text: One comment per attempt, never per gate
  paths:
  - src/torve/application/tracker.py
- id: D-8.8
  grade: ASSUMED
  text: GitHub Issues ships first — the forge the team already lives in, proven on the lab
    repository. Resolved while draft 2026-08-23
  paths:
  - src/torve/adapters/tracker/github.py
budget:
  iterations: null
  wallclock_minutes: null
  tokens: null
tier: executor
