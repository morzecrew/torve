# Minted by the engine at intake — drafting follows the request.
id: T-0286
role: draft
intent: "T-0280's review of T-0279 recorded two major findings that were never addressed,\
  \ both in the revision feedback record. First: capture_feedback unconditionally\
  \ unlinks feedback-threads.json and rewrites feedback.md (src/torve/application/feedback.py:109-110),\
  \ so an in-run blocker capture (src/torve/application/runner.py:1754) destroys the\
  \ pending pull-request reply addresses and the human critique the run was dispatched\
  \ to address; the landing that lands the revised candidate then answers zero review\
  \ threads, against D-5.14 and A-41, and the addresses are gone rather than retried.\
  \ Second: state.escalate(EscalationReason.BLOCKER_FINDING, ...) at src/torve/application/runner.py:1774\
  \ sits outside the 'if spent < budget:' block and calls no capture_feedback, so\
  \ once the blocker-revision budget is spent the record left on disk still holds\
  \ the previous round's critique and the pre-revision diff while the branch carries\
  \ the newer candidate \u2014 the opposite of the accumulated critique D-43.5 says\
  \ a post-escalation re-dispatch inherits. Fix both so that a capture preserves reply\
  \ addresses it did not itself supersede, and so the blockers that cause an escalation\
  \ reach the record before the run ends."
decisions: []
budget:
  iterations: 3
tier: planner
schema_version: 1
