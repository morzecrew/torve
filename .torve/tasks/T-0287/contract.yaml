# Minted by the engine at intake — drafting follows the request.
id: T-0287
role: draft
intent: "T-0280's review of T-0279 recorded two major findings that were never addressed,\
  \ both in the revision feedback record. First: capture_feedback unconditionally\
  \ unlinks feedback-threads.json and rewrites feedback.md (src/torve/application/feedback.py),\
  \ so an in-run blocker capture in src/torve/application/runner.py destroys the pending\
  \ pull-request reply addresses the landing needs; the landing that lands the revised\
  \ candidate then answers zero review threads, against D-5.14 and A-41. Second: the\
  \ state.escalate(EscalationReason.BLOCKER_FINDING, ...) call in src/torve/application/runner.py\
  \ sits outside the 'if spent < budget:' block and calls no capture_feedback, so\
  \ once the blocker-revision budget is spent the record left on disk still holds\
  \ the previous round's critique and the pre-revision diff while the branch carries\
  \ the newer candidate \u2014 the opposite of the accumulated critique D-43.5 says\
  \ a post-escalation re-dispatch inherits. Fix both: a capture must preserve reply\
  \ addresses it did not itself supersede, and the blockers that cause an escalation\
  \ must reach the record before the run ends."
decisions: []
budget:
  iterations: 3
tier: planner
schema_version: 1
